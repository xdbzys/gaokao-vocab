# 背群英 App 打包说明

这个项目已经转换成安卓手机 App 工程，核心功能已经同步到 `android` 原生目录。

## 已完成

- 手机端简约实用界面
- 单词 / 短语分展示
- 简洁 / 完整考点模式
- 英文选中文、中文选英文、闪卡三种背诵方式
- 发音播放
- 自定义词库，可改名
- TXT、复制粘贴、图片识别、PDF、PPTX 导入
- 安卓 Capacitor 原生工程
- 关联词算法优化（避免误关联长得像但无词源关系的单词）
- 易混词功能增强（添加 tone/stone 等更多易混词对）
- 词库来源标注（显示每个单词来自哪个词库）
- 高考5000词背景色修复（仅多词库共有时显示蓝底）
- 背诵页新单词自动发音开关（与跳转自动发音合并）
- 兼容性增强（低端设备缓存优化、Android 10 兼容）

## 在电脑上生成 APK

1. 安装 Android Studio。
2. 用 Android Studio 打开项目里的 `android` 文件夹。
3. 等待 Gradle 自动下载完成。
4. 点击顶部菜单 `Build`。
5. 选择 `Build Bundle(s) / APK(s)`。
6. 选择 `Build APK(s)`。
7. 生成的 APK 一般在：

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

## 命令行打包

如果电脑已经配置好 Android Studio、JDK 和 Android SDK，也可以在项目根目录执行：

```bash
npm install
npm run build
npx cap sync android
cd android && ./gradlew assembleDebug
```

## 环境要求

- JDK 17+（推荐 JDK 21）
- Android SDK（compileSdk 36, minSdk 24, build-tools 36.0.0）
- Node.js 18+
- Gradle 8.14.3（gradlew 会自动下载）
