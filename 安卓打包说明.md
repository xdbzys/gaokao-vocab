# 背群英 App 打包说明

这个项目已经转换成安卓手机 App 工程，核心功能已经同步到 `android` 原生目录。

## 已完成

- 手机端简约实用界面
- 单词 / 短语分开展示
- 简洁 / 完整考点模式
- 英文选中文、中文选英文、闪卡三种背诵方式
- 发音播放
- 自定义词库，可改名
- TXT、复制粘贴、图片识别、PDF、PPTX 导入
- 安卓 Capacitor 原生工程

## 在电脑上生成 APK

1. 安装 Android Studio。
2. 用 Android Studio 打开项目里的 `android` 文件夹。
3. 等待 Gradle 自动下载完成。
4. 点击顶部菜单 `Build`。
5. 选择 `Build Bundle(s) / APK(s)`。
6. 选择 `Build APK(s)`。
7. 生成的 APK 一般在：

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

## 命令行打包

如果电脑已经配置好 Android Studio、JDK 和 Android SDK，也可以在项目根目录执行：

```bash
npm install
npm run android:apk
```

## 当前未直接生成 APK 的原因

当前环境无法连接 Gradle 官方下载地址，导致 `./gradlew assembleDebug` 无法下载 Gradle 8.14.3，所以没有办法在这里直接生成 APK 文件。项目本身已经完成安卓化，换到能正常访问 Gradle 的电脑即可生成 APK。
